{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * ImageShop plugin for Craft CMS 3.x
 *
 * ImageShop Settings.twig
 *
 * @author    WebDNA
 * @copyright Copyright (c) 2022 WebDNA
 * @link      https://webdna.co.uk
 * @package   ImageShop
 * @since     2.0.0
 */
#}

{% import "_includes/forms" as forms %}

{{ forms.autosuggestField({
    label: 'Token'|t,
    instructions: 'Imageshop token',
    id: 'token',
    name: 'token',
    suggestEnvVars: true,
    required: true,
    value: settings['token']})
}}

{{ forms.autosuggestField({
    label: 'Key'|t,
    instructions: 'Imageshop private key',
    id: 'key',
    name: 'key',
    suggestEnvVars: true,
    required: true,
    value: settings['key']})
}}

{% set languages = [
  {
    label: 'Norwegian'|t,
    value: 'nb-NO'
  }, {
    label: 'English'|t,
    value: 'en-US'
  }
] %}

{{ forms.selectField({
    label: 'Language'|t,
    id: 'language',
    name: 'language',
    options: languages,
    value: settings['language']})
}}

{{ forms.selectField({
    label: 'Imageshop field used to generate opengraph image'|t,
    instructions: 'This will work only if the SEOmatic plugin is installed. The field must be assigned to the element (such as an entry) for which you want to generate the OpenGraph image using ImageShop. The first image from the field will be used.',
    id: 'openGraphFieldId',
    name: 'openGraphFieldId',
    options: settings.getOpengraphFieldOptions(),
    value: settings.openGraphFieldId})
}}

{{ forms.selectField({
    label: 'Global set which will be used as source for the default opengraph image.'|t,
    instructions: 'This will work only if the SEOmatic plugin is installed. This global must have imageshop field assigned nd will be used only of current element does not have its own specific opengraph defined by its imagshop field.',
    id: 'openGraphGlobalId',
    name: 'openGraphGlobalId',
    options: settings.getOpengraphGlobalOptions(),
    value: settings.openGraphGlobalId})
}}
