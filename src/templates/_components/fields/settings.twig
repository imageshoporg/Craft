{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * Imageshop plugin for Craft CMS
 *
 * ImageShopField Field Settings
 *
 * @author    Imageshop
 * @copyright Copyright (c) 2022 Imageshop
 * @link      https://www.imageshop.org
 * @package   Imageshop
 * @since     2.0.0
 */
#}

{% import "_includes/forms" as forms %}

{# {{ forms.textField({
    label: 'Input text',
    instructions: 'Text on the selection button',
    name: 'buttonText',
    value: 'buttonText'|t('imageshop')
}) }} #}

{{ forms.textField({
    label: 'Sizes'|t('imageshop-da'),
    instructions: 'Predefined sizes the user can choose from.'|t('imageshop-da'),
    name: 'sizes',
    value: field['sizes']
}) }}

{{ forms.lightswitchField({
    label: 'Edit description before insert?'|t('imageshop-da'),
    instructions: 'Indicates whether the description field should be editable when inserting an image.'|t('imageshop-da'),
    name: 'showDescription',
    on: field['showDescription']
}) }}

{{ forms.lightswitchField({
    label: 'Show Crop Dialogue?'|t('imageshop-da'),
    instructions: 'Indicates whether the crop dialogue should be shown.'|t('imageshop-da'),
    name: 'showCropDialogue',
    on: field['showCropDialogue']
}) }}

{{ forms.lightswitchField({
    label: 'Show Size Dialogue?'|t('imageshop-da'),
    instructions: 'Indicates whether the size dialogue should be shown.'|t('imageshop-da'),
    name: 'showSizeDialogue',
    on: field['showSizeDialogue']
}) }}

{{ forms.lightswitchField({
    label: 'Allow multiple?'|t('imageshop-da'),
    instructions: 'Indicates whether the field should allow multiple images'|t('imageshop-da'),
    name: 'allowMultiple',
    on: field['allowMultiple']
}) }}