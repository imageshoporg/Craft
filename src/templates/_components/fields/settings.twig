{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * Imageshop plugin for Craft CMS
 *
 * ImageShopField Field Settings
 *
 * @author    WebDNA
 * @copyright Copyright (c) 2022 WebDNA
 * @link      https://webdna.co.uk
 * @package   Imageshop
 * @since     2.0.0
 */
#}

{% import "_includes/forms" as forms %}

{# {{ forms.textField({
    label: 'Input text',
    instructions: 'Text on the selection button',
    name: 'buttonText',
    value: 'buttonText'|t('imageshop')
}) }} #}

{{ forms.textField({
    label: 'Sizes'|t('imageshop-dam'),
    instructions: 'Predefined sizes the user can choose from.'|t('imageshop-dam'),
    name: 'sizes',
    value: field['sizes']
}) }}

{{ forms.lightswitchField({
    label: 'Edit description before insert?'|t('imageshop-dam'),
    instructions: 'Indicates whether the description field should be editable when inserting an image.'|t('imageshop-dam'),
    name: 'showDescription',
    on: field['showDescription']
}) }}

{{ forms.lightswitchField({
    label: 'Show Crop Dialogue?'|t('imageshop-dam'),
    instructions: 'Indicates whether the crop dialogue should be shown.'|t('imageshop-dam'),
    name: 'showCropDialogue',
    on: field['showCropDialogue']
}) }}

{{ forms.lightswitchField({
    label: 'Show Size Dialogue?'|t('imageshop-dam'),
    instructions: 'Indicates whether the size dialogue should be shown.'|t('imageshop-dam'),
    name: 'showSizeDialogue',
    on: field['showSizeDialogue']
}) }}

{{ forms.lightswitchField({
    label: 'Allow multiple?'|t('imageshop-dam'),
    instructions: 'Indicates whether the field should allow multiple images'|t('imageshop-dam'),
    name: 'allowMultiple',
    on: field['allowMultiple']
}) }}